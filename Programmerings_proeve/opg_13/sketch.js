let num = [];
let tested = [];

function setup() {
  createCanvas(400, 400);
  for (let i = 0; i < 2000; i++) {
    num.push(floor(random(0, 100)));
  }

  for (let i = 0; i < num.length; i++) {}
}

function draw() {}
